{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tour list</h1>

    <table class="records_list" border=2>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>City</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tour,categories in entities %}
            <tr>
                <td><a href="{{ path('tour_show', { 'id': loop.index }) }}">{{ loop.index }}</a></td>
                <td>{{ tour }}</td>
                <td>{{ tour.city }}</td>
                <td>
				        {% for category in categories %}
						   {{ category }}
						{% endfor %}
				</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tour_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tour_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tour_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
