{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TourCategoryConn list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tour</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tourcategoryconn_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tour }}</td>
                <td>{{ entity.category }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tourcategoryconn_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tourcategoryconn_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tourcategoryconn_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
